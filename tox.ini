[tox]
envlist = py311, py312, lint, type
skip_missing_interpreters = true

[testenv]
extras = dev
commands = pytest {posargs}

[testenv:lint]
commands = ruff check komin_terminal tests

[testenv:type]
commands = mypy komin_terminal
